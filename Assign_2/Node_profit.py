class Node_profit:

    def __init__(self, type, origin, destination, profit, binNumber, stage, cargo):
        self.type   = type
        self.origin = origin
        self.destination = destination
        self.profit = profit
        self.binNum = binNumber
        self.stage  = stage
        self.cargo  = cargo

